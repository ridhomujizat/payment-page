const config = {
    baseURL: import.meta.env.VITE_APP_API_URL,
    basic: {
      username: import.meta.env.VITE_APP_BASIC_USER,
      password: import.meta.env.VITE_APP_BASIC_PASS,
    }
  };
  
export default config